description = "ARIG Association - Odin utils"

apply plugin: 'org.springframework.boot'

dependencies {
	compile project(':odin-parent:odin-robot')

	compile "org.springframework.shell:spring-shell-starter:${springShellVersion}"
}
